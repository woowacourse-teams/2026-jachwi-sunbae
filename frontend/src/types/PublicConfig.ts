export type PublicConfig = {
  apiBaseUrl: string;
  mapProviderMode?: 'demo' | 'naver';
  naverMapClientId?: string;
  metaPixelId?: string;
  posthogProjectToken?: string;
  posthogHost?: string;
};
