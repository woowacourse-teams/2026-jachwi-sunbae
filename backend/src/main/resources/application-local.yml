spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:jachwi_sunbae}?allowPublicKeyRetrieval=true&useSSL=false&connectionTimeZone=%2B00:00&forceConnectionTimeZoneToSession=true&preserveInstants=true
    username: ${DB_USERNAME:jachwi_sunbae}
    password: ${DB_PASSWORD:local_password}
    driver-class-name: com.mysql.cj.jdbc.Driver

photo:
  storage:
    endpoint: ${PHOTO_STORAGE_ENDPOINT:http://localhost:9000}
    access-key: ${PHOTO_STORAGE_ACCESS_KEY:local-photo-access-key}
    secret-key: ${PHOTO_STORAGE_SECRET_KEY:local-photo-secret-key}
