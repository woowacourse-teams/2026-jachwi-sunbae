spring:
  sql:
    init:
      mode: always
      schema-locations: classpath:db/init/001-schema.sql
      data-locations: classpath:db/init/002-seed.sql

auth:
  jwt:
    secret: test-jwt-secret-must-be-at-least-32-bytes
    issuer: jachwi-sunbae
    audience: jachwi-sunbae-api
    access-token-seconds: 43200
  nickname:
    max-failures: 5
    failure-window-seconds: 600

app:
  cors:
    allowed-origins: http://localhost:3000

photo:
  storage:
    endpoint: http://localhost:9000
    region: us-east-1
    bucket: test-photos
    access-key: test-access-key
    secret-key: test-secret-key

map:
  provider:
    mode: demo

demo:
  seed:
    enabled: false
