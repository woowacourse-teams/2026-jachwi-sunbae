# MVP2 제품·구현 결정 대장

- 상태: 구현 기준 v1

## 사용 방법

MVP2는 빠르게 실행 가능한 결과를 우선합니다. 비차단적인 모호함은 아래 기본 결정을 따르고 구현 중 새 결정은 다음 번호로 추가합니다. 사용자가 결과를 사용한 뒤 방향을 바꾸면 기존 행을 지우지 않고 상태를 `대체됨`으로 바꾼 뒤 새 결정을 추가합니다.

| ID | 상태 | 결정 | 이유와 영향 |
| --- | --- | --- | --- |
| `PD-001` | 채택 | 자료 충돌 시 사용자 확정 결정 → 최종 와이어프레임 → 실제 MVP1 구현 → 전달 문서 → 추론 순으로 판단합니다. | 화면 의도와 실제 기준선을 모두 보존합니다. |
| `PD-002` | 채택 | 대표 사진을 사용자가 직접 지정합니다. | `03-1` 별 버튼과 이미 구현된 MVP1 API를 따릅니다. |
| `PD-003` | 채택 | `CORE`는 생성 시 자동 포함하지만 편집 시 제거할 수 있습니다. | 빠른 개인화와 현재 구현을 유지합니다. 최소 1개 제한은 지킵니다. |
| `PD-004` | 채택 | 매물 수정은 `PUT` 전체 교체이고 미입력 금액은 0입니다. | 현재 API·도메인과 맞추고 부분 수정의 모호함을 제거합니다. |
| `PD-005` | 채택 | 관리비는 매물 컬럼이 아니라 기본 메모 항목으로 둡니다. | 와이어프레임이 메모에 관리비를 배치하며 중복 저장을 피합니다. |
| `PD-006` | 채택 | 매물에 도로명·지번 주소와 WGS84 위도·경도를 저장합니다. | 지도 핀 복원과 주소 표시를 안정화합니다. |
| `PD-007` | 채택 | 생성·수정·사진·메모·체크 변경이 `lastActivityAt`을 갱신합니다. | 와이어프레임의 최신순을 정의합니다. |
| `PD-008` | 채택 | DB 종속 행은 FK cascade를 기본으로 하고 서비스가 객체 저장소 삭제를 조정합니다. | DB 누락 삭제를 줄이되 S3는 트랜잭션 밖 보상 처리가 필요합니다. |
| `PD-009` | 채택 | 사진은 30장·5MiB의 실제 제한이며 JPEG·PNG·WebP만 지원합니다. | HEIC 브라우저 호환과 서버 변환 비용을 MVP2 v1에서 제외합니다. |
| `PD-010` | 채택 | 지도·주소·주변 시설은 Kakao Maps와 Kakao Local REST API로 통일합니다. | 국내 주소 품질과 단일 계정 설정을 우선합니다. |
| `PD-011` | 대체됨 | 외부 연동은 인증·지도 모두 `demo`와 `live` adapter로 분리하고 로컬 기본은 `demo`입니다. | `PD-045`가 인증의 외부 adapter를 없애고 닉네임 인증으로 대체합니다. 지도 adapter 결정은 유지합니다. |
| `PD-012` | 채택 | Kakao REST 키는 백엔드 전용이고 장소 응답은 10분 캐시합니다. | 키 노출과 쿼터 낭비를 줄입니다. |
| `PD-013` | 채택 | 기본체크리스트는 활성 `CORE`의 가상 preset이며 사용자 목록 행을 만들지 않고 바로 매물에 적용합니다. | 불필요한 개인 체크리스트 생성을 막습니다. |
| `PD-014` | 대체됨 | 비교표 받기는 추천 없는 CSV 내보내기입니다. | `PD-044`가 선택형 전체 기록 PDF로 대체합니다. |
| `PD-015` | 채택 | 브라우저 세션 복구는 `sessionStorage` access token으로 하고 refresh token은 도입하지 않습니다. | MVP 속도와 새로고침 사용성을 절충합니다. |
| `PD-016` | 채택 | `13-1`의 기존 매물 핀은 `13-3` 주변 분석으로 이동합니다. | 최종 `.pen` 파일과 별도 화면 이미지를 따릅니다. |
| `PD-017` | 채택 | `03-3`을 매물 정보 수정의 공식 화면 번호로 사용합니다. | 파일명과 최종 `.pen` 표기를 통일합니다. |
| `PD-018` | 대체됨 | 주변 시설은 Kakao 장소 데이터만 사용하고 공공데이터포털 별도 연동은 보류합니다. | `PD-040`이 Kakao에 없는 일반 버스정류소를 TAGO로 보완합니다. |
| `PD-019` | 채택 | 상세 API 계약은 구현 전 Markdown을 기준으로 하고 구현 후 생성 OpenAPI JSON을 실제 정본으로 전환합니다. | 계약 우선 설계와 Springdoc 실제 결과의 중복을 관리합니다. |
| `PD-020` | 채택 | 다음 구현 작업은 로컬 완성과 배포 준비까지만 수행하고 AWS 생성·실배포는 하지 않습니다. | 저장소 규칙과 사용자 선행 결정을 지킵니다. |
| `PD-021` | 채택 | Kakao 유료 API·비즈월렛을 자동 활성화하지 않습니다. | 비용 발생은 사용자의 별도 승인 대상입니다. 무료 쿼터 부족 시 데모 모드로 유지합니다. |
| `PD-022` | 대체됨 | `demo` 인증에서는 `00`의 주 버튼 문구를 `데모로 시작하기`로 바꾸고 같은 위치에서 데모 로그인을 실행합니다. | `PD-045`가 운영·로컬을 하나의 닉네임 시작 화면으로 통합합니다. |
| `PD-023` | 채택 | `13-3`의 `전체`는 반경 2km와 전체 카테고리로 필터를 초기화합니다. | 공급자에 무제한 조회를 요청하지 않고 `500m·1km·2km` 계약을 유지합니다. |
| `PD-024` | 채택 | 데모 시드는 회원 이메일을 기준으로 멱등하게 만들고 이미 매물이 있으면 사용자 데이터를 덮어쓰지 않습니다. | 로컬 재시작 때 샘플을 중복 생성하거나 사용자의 확인 데이터를 잃지 않습니다. |
| `PD-025` | 채택 | 사진 콘텐츠는 공개 URL·presigned URL 없이 소유자 검증 백엔드 endpoint로 전달합니다. | MinIO와 S3 버킷을 비공개로 유지하고 같은 프론트 계약을 사용합니다. |
| `PD-026` | 채택 | 지도에서 위치를 누르면 역지오코딩한 도로명·지번 주소와 좌표를 매물 폼으로 전달합니다. | `13-2 → 02` 흐름에서 주소 문자열과 핀 위치가 어긋나지 않게 합니다. |
| `PD-027` | 대체됨 | 새 사용자 체크리스트는 시작 방식 선택 없이 해당 단계의 원룸 제공 항목을 즉시 불러오며 빈 목록 시작은 제공하지 않습니다. | `PD-028`이 초깃값을 `CORE`로 구체화해 이 결정을 대체합니다. |
| `PD-028` | 채택 | 새 사용자 체크리스트는 해당 단계의 활성 `CORE`만 먼저 보여주고 활성 `OPTIONAL`은 체크 항목 추가 화면에서 선택합니다. | 기본 질문과 선택 질문의 역할을 구분하면서 빈 목록 없는 단일 생성 흐름을 유지합니다. |
| `PD-029` | 채택 | 매물 상세의 세 단계 카드는 적용 여부와 관계없이 체크리스트 선택 화면으로 이동합니다. | 현재 적용 목록을 확인하면서 다른 목록 또는 기본체크리스트로 바꾸는 와이어프레임 `03 → 04` 흐름을 따릅니다. |
| `PD-030` | 채택 | 매물 상세 미리보기 사진은 대표 여부와 관계없이 동일한 정사각형으로 표시합니다. | 대표 여부가 달라도 미리보기 카드는 같은 시각 비중을 가져야 사진 비교가 쉽습니다. |
| `PD-031` | 채택 | 매물 상세의 전체 진행 집계와 별도로 세 단계 행에 단계별 진행률과 상태 수를 표시합니다. | 사용자가 단계마다 상태를 비교하고 진입 전에 확인할 수 있게 합니다. |
| `PD-032` | 채택 | 지도 시설 카테고리는 단일 선택이 아니라 다중 선택으로 동작합니다. | 사용자가 병원·교통 등 원하는 시설 조합을 한 화면에서 비교할 수 있고 매물·현재 위치 핀은 유지됩니다. |
| `PD-033` | 대체됨 | `13-3` 지도에는 카테고리별 개수 요약 핀을 먼저 표시하고 실제 시설 목록은 하단에서 펼쳐 봅니다. | `PD-037`이 임의 위치 요약 핀을 실제 좌표 군집으로 대체합니다. |
| `PD-034` | 채택 | 주소 검색은 기본으로 접고 검색 버튼 또는 현재 위치 조회 실패 때만 엽니다. | 와이어프레임의 지도 가시 영역을 유지하면서 위치 권한이 없는 사용자에게 대체 입력을 제공합니다. |
| `PD-035` | 채택 | 신규 지도 등록은 현재 위치를 우선하고 매물 수정은 저장된 매물 위치를 우선합니다. | 신규 등록의 이동량을 줄이되 기존 매물 주소를 수정할 때 위치가 임의로 바뀌지 않게 합니다. |
| `PD-036` | 대체됨 | `13-1`의 실제 시설 핀은 카테고리별 거리순 2개까지만 표시하고 전체 집계와 `13-3` 목록은 줄이지 않습니다. | `PD-037`이 실제 장소를 자르지 않고 확대 단계별로 군집화하는 방식으로 대체합니다. |
| `PD-037` | 채택 | `13-1`과 `13-3`은 공급자가 반환한 모든 장소를 실제 좌표로 표시하되, 축소 단계에서는 가까운 장소만 좌표 중심 군집으로 묶고 확대하면 개별 핀으로 풉니다. | 버스정류장 같은 실제 시설 위치를 확인하면서 모바일 지도 과밀도 줄입니다. |
| `PD-038` | 채택 | Kakao 장소 검색은 페이지당 15개씩 최대 세 페이지를 조회하고 응답의 `is_end`에서 중단합니다. | 공급자가 노출하는 검색별 최대 45개를 사용하되 무제한 외부 호출은 피하며 기존 10분 캐시를 유지합니다. |
| `PD-039` | 채택 | `13-1`도 전체·500m·1km·2km를 제공하고 조회 반경은 현재 위치 또는 주소로 선택한 위치에 고정합니다. | 지도를 탐색할 때 분석 기준이 몰래 바뀌지 않으며 반경을 선택하면 기준 위치와 전체 범위를 다시 확인할 수 있습니다. |
| `PD-040` | 채택 | Kakao 교통 결과는 지하철 `SW8`로 한정하고, 일반 버스정류소는 선택적 TAGO 어댑터로 중심 500m 실제 정류소를 합칩니다. | Kakao 키워드 검색은 일반 정류소 전체를 제공하지 않습니다. TAGO는 무료·자동승인이며, 장애 시 다른 지도 결과는 유지합니다. |
| `PD-041` | 채택 | `13-1`과 `13-3`은 시설 카테고리를 모두 끈 채 시작하고, 요약 시트에는 전체 개수를 유지하며 선택한 카테고리만 핀과 스크롤 목록에 표시합니다. | 초기 지도 과밀을 줄이면서 사용자가 실제 개수를 보고 필요한 시설만 조합해 탐색할 수 있습니다. |
| `PD-042` | 채택 | 시설 핀과 우측 필터는 같은 카테고리 아이콘을 사용하고, 개별 핀은 목록과 같은 상세를 열며 군집 핀은 지도를 확대합니다. | 아이콘 의미와 상세 정보 출처를 일치시키고 모바일 지도 안에서 시설을 바로 식별·탐색할 수 있습니다. Kakao 바탕지도의 POI는 조회 결과로 간주하지 않습니다. |
| `PD-043` | 채택 | 매물 목록 카드는 미완료 배지와 전체 통합 바를 제거하고 발견 경로와 1·2·3단계별 집계를 표시합니다. | 순차적·선택적으로 사용하는 단계를 통합 진행률이 왜곡하지 않고 매물을 어디서 발견했는지도 목록에서 즉시 확인합니다. |
| `PD-044` | 채택 | 비교표 받기는 매물 2~5개를 선택하고 저장한 기본 정보·사진·메모·3단계 체크 기록 전체를 PDF로 제공합니다. | 노션과 A4로 따로 옮겨 비교하는 부담을 줄이되 점수·추천·자동 판단을 추가하지 않습니다. PDF의 첫 장은 나란한 요약 비교, 뒷장은 매물별 전체 기록입니다. |
| `PD-045` | 채택 | 외부 계정 연결을 없애고 서비스 전체에서 닉네임으로 시작합니다. 첫 사용 때 비밀번호는 선택이며, 비밀번호 없는 같은 닉네임은 기록을 공유하고 비밀번호를 둔 닉네임만 보호합니다. | 실험 참여자의 개인정보 제공 부담과 로그인 병목을 줄입니다. 공유 닉네임은 누구나 조회·수정·삭제할 수 있음을 화면에 알리고, 나중의 비밀번호 추가·변경·복구는 제공하지 않아 소유권 전환의 모호함을 피합니다. |
| `PD-046` | 채택 | 제공 체크 문항을 실사용 기준의 `CORE` 21개·`OPTIONAL` 32개로 교체하고 사용자가 200자 이하의 질문을 직접 추가할 수 있게 합니다. 직접 질문은 시스템 ID가 없는 `OPTIONAL` 스냅샷으로 저장하며 모든 단계에서 `괜찮음·주의·미확인` 상태를 사용합니다. | 제공 문항만으로 표현하지 못하는 개인 기준을 현장에서 바로 기록합니다. 이전 18개 제공 문항은 비활성화해 기존 스냅샷과 FK를 보존하고, 체크리스트 교체 시 시스템 문항은 ID로, 직접 질문은 같은 문구로 상태와 메모를 승계합니다. |
| `PD-047` | 채택 | 체크리스트 편집의 항목 추가 버튼은 확인 순서보다 먼저 노출하고 편집 항목은 개별 카드로 구분합니다. 매물 체크 진행 화면의 중복 교체 버튼은 제거하고 매물 상세의 단계 선택 화면에서만 목록을 바꿉니다. | 긴 편집 목록에서도 항목 추가 진입점을 즉시 찾게 하고, 적용 목록을 바꾸는 진입 경로를 하나로 줄여 혼동을 낮춥니다. |
| `PD-048` | 채택 | 체크리스트 만들기·저장 버튼은 확인 순서보다 먼저, 항목 선택의 취소·추가 버튼은 검색 결과보다 먼저 노출합니다. | 모바일에서 긴 항목 목록을 끝까지 내려야 완료 액션을 발견하는 문제를 없애고 화면과 키보드 탐색 순서를 일치시킵니다. |
| `PD-049` | 대체됨 | 외부 사용자 모집용 QR과 링크는 인증 없이 여는 `/intro`로 통일하고, 서비스 문제·가치·사용법을 설명한 뒤 기존 닉네임 시작 화면으로 연결합니다. 채널별 측정과 별도 신청 폼은 넣지 않습니다. | `PD-056`이 공통 QR·소개·별도 신청 폼 제외는 유지하면서 동의 기반 광고 귀속 측정을 추가합니다. |
| `PD-050` | 채택 | 공개 소개 화면의 첫 가치는 일반적인 기록 편의가 아니라 월세만 볼 때 놓치는 관리비·보증금·수리비와 임차인 관점으로 설명합니다. 금액은 조건을 밝힌 계산 예시로만 쓰고 법률·지원 자격·하자 판정이나 손해 방지를 보장하지 않습니다. | 이전 가설 검증에서 반응이 컸던 금전 손실 문제를 외부 사용자에게 즉시 전달하면서도 현재 제품의 기록·체크·비교 범위보다 큰 효용을 약속하지 않습니다. |
| `PD-051` | 채택 | 공개 소개의 첫 문장은 짧은 임장 중 놓친 문제가 거주 중 반복된다는 문제와 돈을 잃지 않는 선택을 함께 말합니다. 2년 총비용 예시와 매물별 사진·메모·체크·주변 시설 기록 예시를 상하로 연이어 보여줍니다. | 금전 손실의 긴장감은 유지하면서도 자취선배가 실제로 제공하는 핵심 가치가 매물의 기록과 관리임을 첫 화면에서 구체적으로 연결합니다. |
| `PD-052` | 채택 | 체크리스트에는 자취선배가 제공하는 `CORE`·`OPTIONAL` 문항만 담고 사용자가 질문 문장을 직접 추가하거나 수정하는 기능은 제공하지 않습니다. 이전 버전에서 저장된 직접 질문은 데이터 손실을 막기 위해 문구를 바꾸지 않은 채 유지·정렬·제거할 수 있습니다. | 외부 사용자 실험 전에 편집 선택지를 줄이고 제공 문항 자체의 적합성을 먼저 검증합니다. `PD-046`의 직접 질문 추가 부분을 대체하며 53개 제공 문항과 세 단계 상태·메모 정책은 유지합니다. |
| `PD-053` | 채택 | 공개 소개의 사용 영상은 세 편을 한 화면에서 한 편씩 음소거·2배속 자동 재생하고 마지막 편 뒤 처음부터 반복합니다. 화면 밖에서는 멈추고 사용자가 일시정지할 수 있으며 동작 줄이기 환경에서는 정지 상태로 시작합니다. | 세 영상을 동시에 작게 보여주지 않아 모바일 화면의 입력과 결과를 읽을 수 있게 하고 별도 재생 조작 없이 전체 흐름을 전달합니다. 화면 밖 재생과 강제 동작은 줄여 데이터·기기 자원과 접근성을 함께 지킵니다. |
| `PD-054` | 채택 | 사용 영상의 세 순서 카드를 선택 버튼으로 제공하고 누른 영상의 처음으로 즉시 전환해 재생합니다. 자동 순환과 현재 선택 표시는 유지합니다. | 관심 있는 흐름을 기다리지 않고 바로 볼 수 있게 하면서도 처음 방문한 사용자에게는 전체 자동 시연을 그대로 제공합니다. |
| `PD-055` | 대체됨 | 로그인 사용자가 비교 화면을 열 때마다 회원, 진입 시각, 그 시점의 보유 매물 수를 RDS에 이벤트로 저장합니다. 소개 화면과 모집 채널은 추적하지 않습니다. | `PD-056`이 RDS 비교 이벤트는 유지하면서 동의 기반 Meta 측정을 추가합니다. |
| `PD-056` | 채택 | 모집 QR과 링크는 공통 `/intro`를 사용하고 별도 신청 폼·채널별 URL·UTM은 만들지 않습니다. 로그인 사용자의 비교 화면 진입은 RDS에 계속 저장합니다. 사용자가 명시적으로 동의한 경우에만 Meta Pixel을 불러와 SPA 페이지 방문, 신규 닉네임 생성, 첫 매물 등록 이벤트를 기록합니다. 닉네임·비밀번호·주소·사진·메모·체크 내용은 보내지 않고, 거부하거나 철회해도 서비스 기능을 제한하지 않습니다. | 광고 랜딩 조회가 실제 시작과 기록으로 이어지는지 귀속하면서 개인정보 제공 부담을 줄입니다. `PD-049`와 `PD-055`를 대체하되 공통 소개 진입점과 RDS 제품 행동 집계는 유지합니다. |

## 구현 중 멈춰야 하는 경우

다음은 비차단 결정으로 처리하지 않습니다.

- 결제·과금 또는 유료 API 활성화
- 실제 AWS 자원 생성·도메인 연결·외부 배포
- 개인정보 처리 범위를 넓히는 기능
- 복구하기 어려운 데이터 삭제나 저장소 이력 재작성

그 밖의 UI 세부값·오류 문구·기술 구현 선택은 구현자가 진행하고 이 대장에 기록합니다.
