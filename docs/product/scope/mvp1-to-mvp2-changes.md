# MVP1에서 MVP2로의 변경점

- 문서 성격: 파생
- 대조 대상: [`mvp1-baseline.md`](mvp1-baseline.md), [`mvp2-scope.md`](mvp2-scope.md), [MVP2 와이어프레임](../../design/wireframes/README.md)

## 화면 기준 변경표

| 화면             | MVP1 기준선                        | MVP2 변경                                                              | 영향          |
| ---------------- | ---------------------------------- | ---------------------------------------------------------------------- | ------------- |
| `00` 로그인      | Google 로그인, 메모리 JWT          | 이름·닉네임과 선택 비밀번호로 즉시 시작, 세션 복구                     | FE·인증·DB    |
| `01` 매물 목록   | ID 역순, 주소·최근 활동 없음       | 주소·사진 수·금액·단계별 진행 현황·발견 경로·최신순, 선택형 PDF 비교표 | DB·API·FE     |
| `02` 매물 등록   | 이름·보증금·월세·발견 경로         | 주소 검색·지도 선택과 좌표, 관리비는 메모에서 관리                     | DB·API·지도   |
| `03` 매물 상세   | 기본 정보·사진·메모·진행 현황      | 새 카드 구조, 주소, 대표 사진과 3단계 요약                             | API·FE        |
| `03-1` 사진      | 조회·삭제·대표 지정, 업로드 미구현 | 실제 업로드, 30장, 대표 지정 UI                                        | 객체 저장·API |
| `03-2` 메모      | 시스템 메모 스냅샷·자유 메모       | 입주 가능일·방 옵션·관리비·방문 일정 기본 양식                         | 시드·FE       |
| `03-3` 매물 수정 | 전체 교체                          | 주소·좌표 포함 전체 교체                                               | DB·API        |
| `04` 적용 선택   | 단계별 사용자 체크리스트 적용      | 시스템 기본 체크리스트와 사용자 목록을 함께 표시                       | API·FE        |
| `05` 체크 진행   | 상태·메모 독립 저장                | 상태 즉시 저장, 한 항목씩 메모 편집·저장·취소                          | FE 중심       |
| `06` 체크 탭     | 단계 홈                            | 와이어프레임 카드 구조로 변경                                          | FE            |
| `07` 내 목록     | 목록·삭제                          | 단계 탭, 수정·삭제·생성, 빈 상태                                       | FE            |
| `08` 편집        | 생성·수정 화면이 분리됨            | 하나의 편집 화면, 순서 변경·삭제·저장                                  | FE·API 유지   |
| `09` 항목 편집   | 시스템 항목 검색                   | 다중 선택 후 일괄 추가, 검색 빈 상태                                   | FE            |
| `10` 마이        | 회원 조회·로그아웃                 | 새 프로필 카드와 버전                                                  | FE            |
| `13-1` 지도      | 없음                               | 현재 위치, 매물·시설 핀, 카테고리 필터                                 | 신규 통합     |
| `13-2` 위치 선택 | 없음                               | 좌표 선택, 역지오코딩, 등록 화면 전달                                  | 신규 통합     |
| `13-3` 주변 분석 | 없음                               | 반경·카테고리별 장소와 개수                                            | 신규 통합     |

## 도메인 기준 변경

### 인증

- 로컬과 운영 모두 이름·닉네임과 선택 비밀번호 인증을 사용합니다.
- 비밀번호 없는 같은 닉네임은 같은 기록을 공유하고, 비밀번호를 설정한 닉네임은 소유자만 접근합니다.
- MVP2 v1에서도 refresh token은 도입하지 않고 새로고침 시 세션 복구는 `sessionStorage`를 사용합니다. 민감도와 속도를 고려해 12시간 access token과 만료 시각만 저장합니다.

### 매물

- `address`, `road_address`, `latitude`, `longitude`, `last_activity_at`을 추가합니다.
- 관리비는 MVP2 매물 기본 필드로 추가하지 않고 기본 메모 양식에서 기록합니다.
- 금액 미입력은 기존 호환을 위해 0으로 저장하되 응답에서 0을 미입력처럼 표시할지는 프론트가 결정합니다.
- 목록 기본 정렬을 최근 활동 시각, ID 내림차순으로 바꿉니다.

### 사진

- MVP1에서 빠진 업로드를 완성합니다.
- 매물당 30장, 파일당 5MiB를 실제 제한으로 적용합니다.
- 브라우저 호환을 위해 MVP2 v1 업로드는 JPEG·PNG·WebP만 허용합니다. HEIC·HEIF는 서버 변환을 구현하기 전까지 오류로 안내합니다.
- 첫 사진은 대표가 되고 사용자가 다른 사진을 지정할 수 있습니다.

### 체크리스트

- 스냅샷·교체·집계 정책은 유지합니다.
- `CORE`는 생성 시 기본으로 넣지만 사용자가 편집 화면에서 제거할 수 있습니다.
- 제공 문항은 실사용 결과로 다시 정한 `CORE` 21개와 `OPTIONAL` 32개로 교체합니다.
- 사용자는 제공 `OPTIONAL`을 선택해 같은 체크·메모 흐름에서 사용합니다. 사용자 질문 직접 추가는 외부 실험 범위를 줄이기 위해 제공하지 않습니다.

### 지도

- 장소 데이터는 저장하지 않고 요청 시 조회합니다.
- 매물 주소와 좌표만 저장합니다.
- 주변 분석은 네이버 지역 검색 응답을 짧게 캐시하며 장애 시 마지막 화면을 유지하고 재시도 안내를 표시합니다.

## 기준선에서 발견한 구현 결손

- 사진 업로드 백엔드가 없습니다.
- 대표 사진 테이블의 현재 UNIQUE 제약은 매물당 한 장을 DB 수준에서 보장하지 못합니다.
- 일부 하위 테이블 삭제는 애플리케이션 순서에 의존하고 FK cascade가 일관되지 않습니다.
- `last_activity_at`이 없어 최신순 요구를 충족하지 못합니다.
- access token 메모리 저장은 새로고침 뒤 로그인 상태를 유지하지 못합니다.

다음 구현은 이 결손을 버그·기반 작업으로 먼저 해결한 뒤 화면 순서대로 진행합니다.
